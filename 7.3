#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {
	int a=-10, b=10, k=0;
	double array[20];
	srand(time(NULL));
	
	printf("ARRAY A:\n");
	for(int i=0; i<20; ++i) {
		array[i]=(double)rand()*(b - a)/RAND_MAX + a;
		if(array[i]>0) k+=1;
		printf("%lf ",array[i]);
	}
	
	printf("\nARRAY B:\n");
	
	double array1[k];
	int n=0;
	
	for(int i=0; i<20; ++i) {
		if(array[i]>0) {
			array1[n]=array[i];
			n+=1;
		}
	}
	
	for(int i=0; i<k; ++i) printf("%lf ",array1[i]);
}
