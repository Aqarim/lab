#include <stdlib.h>
#include <stdio.h>

int main() {
	int i,k;
	long long n;
	printf("Input n= ");
	scanf("%d",&n);
	
	bool m[n]={0};
	
	for (i=2; i*i<n; i++) {
		for (k=i*i; k<n; k+=i)
			m[k]=1;
	}
	
	for (i=2; i<n; i++) 
		if (m[i]==0) printf("%d\n",i);
}
