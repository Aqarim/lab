#include <stdlib.h>
#include <stdio.h>

int main () {
    double Pi, e, n, k;
	  int x;
	
  	k=1000000;
  	n=3;
  	Pi=1;
	  x=1;
	
	  while (k>1) {
	    if (x%2==0) Pi=Pi+1/n;
		    else Pi=Pi-1/n;
		  k=k-1;
		  n=n+2;
		  x=x+1;
	  }
	
    Pi=4*Pi;
    printf("%lf\n",Pi);
    system("PAUSE");
}
